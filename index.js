const inquirer = require('inquirer');
const fs = require('fs');
const generateMarkdown = require('./util/generateMarkdown');

// Define array of questions for user input
const questions = [
  // Define questions here...
];

// Function to write README file
function writeToFile(fileName, data) {
  // Implement logic to write data to file
}

// Function to initialize app
function init() {
  // Implement logic to prompt user for input using inquirer
  // Collect user responses and pass them to generateMarkdown function
  // Write generated markdown content to a README file
}

// Call init function to start the application
init();
